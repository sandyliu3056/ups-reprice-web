# UPS Reprice Tool — 部署包

從這一份開始看。

---

## 現況(2026-08-20)

`github.com/sandyliu3056/ups-reprice-web` 是 **Public**,1 個 commit,網頁上傳的六個檔案:

```
UPS_Reprice_Tool.py
app.py
engine.cpython-314.pyc
engine.py
requirements.txt
t_web.py
```

**沒有 `ups_billing_tool_config.json`,沒有 `ups_history.sqlite3`。**
議價費率和 104,463 筆帳單明細從來沒有上去過,歷史裡也沒有。最壞的情況已經排除。

還沒確認的四件事列在下面「現在該做什麼」。

---

## 現在該做什麼

### 1. 確認 `engine.py` / `app.py` 有沒有寫死費率 ← 最重要

repo 裡沒有 config,那 web 版的費率從哪來?如果為了讓網頁跑起來把費率寫進
`engine.py`,那合約就是公開的,和上傳 config 沒有兩樣。

打開檔案搜尋這幾個字:`0.85`、`2.55`、`205`、`34`、`Zone`、`rate`。
看到一整排數字就是有問題。

### 2. 移掉原始碼裡的帳號

`UPS_Reprice_Tool.py` 第 4870 行的註解裡引用了一組實際發票號,裡面含你的 UPS
帳號,現在公開可見。開檔跳到那一行,把發票號改成「某一期」即可。

(這份說明刻意不寫出那組號碼 —— 寫了,這個檔案本身就成了下一個洩漏點。)

### 3. 刪掉 `engine.cpython-314.pyc`

`.pyc` 反編譯得回原始碼,等於同一份東西公開兩次。刪掉,並確認 `.gitignore` 生效。

### 4. 看 Deployments

repo 頁面右側寫著 Deployments 1 — 有東西部署出去了。點進去確認是什麼。
如果是 GitHub Pages,站上每個檔案都有公開網址,**之後放進去的任何東西也一樣**。

### 注意

只有 1 個 commit 的情況下,**刪檔案刪不掉歷史** — 刪了會產生第 2 個 commit,
第 1 個裡面還在,照樣抓得到。要真的清掉第 2 項那一行,得刪 repo 重建。

---

## 這個包裡有什麼

### 桌面版打包

| 檔案 | 用途 |
|---|---|
| `部署說明.md` | 完整說明。桌面版要一模一樣,先看這份 |
| `UPS_Reprice_Tool.spec` | PyInstaller 設定。onedir,已收 customtkinter 佈景與 tzdata |
| `build.bat` | 一鍵打包。放在 `UPS_Reprice_Tool.py` 旁邊雙擊 |
| `requirements.txt` | 相依套件。要重現同一個 build 請改用 `pip freeze` 產的 lock 檔 |
| `verify_deploy.py` | 兩台各跑一次,對 config 的 sha256、各項筆數、字型 |

### git 守門

| 檔案 | 用途 |
|---|---|
| `.gitignore` | 擋客戶資料、帳單、憑證、打包產出 |
| `.githooks/pre-commit` | 硬的那一道。`git add -f` 也擋得住,還會掃檔案內容裡的 token |
| `make_sample_config.py` | 從真的 config 產出可以進 git 的樣本 |
| `ups_billing_tool_config.sample.json` | 已經產好的樣本。結構完整、費率全零 |

安裝 hook(在 repo 根目錄跑一次):

```
git config core.hooksPath .githooks
```

用 `.githooks/` 不是 `.git/hooks/` — 後者不會跟著 clone 走,同事那邊就沒有防護。

> `.gitignore` 開頭是點,解壓後在檔案總管可能看不到。
> 檢視 → 顯示 → 隱藏的項目,打勾就出現了。

### 稽核工具

| 檔案 | 用途 |
|---|---|
| `工具/audit_git.py` | 查一個 repo 的完整歷史,含已刪除但還在歷史裡的檔案 |
| `工具/find_my_files.py` | 掃全機:每一份 config 和 history 在哪、哪一份有真資料、有幾個 git repo |

兩支都是唯讀,放哪都能跑,結果另外寫一份 UTF-8 的 txt。

---

## 桌面版部署的三個重點

**一、exe 不是重點,跟著走的檔案才是。**
`ups_billing_tool_config.json` 少了就是全新空白設定,金額整份不一樣。
`ups_history.sqlite3` 少了單期看起來對、跨期 ADJ/SCC 才錯 — 住宅/商業會分類錯。

**二、spec 用 onedir 不是 onefile。**
PyInstaller 6 把 DLL 全收進 `_internal\`,最上層只留 exe,剛好對上 `_app_dir()` 的設計。
onefile 每次啟動都要解壓 pandas,冷開機十幾秒。

**三、字型只影響外觀,不影響金額。**
沒裝就掉回正黑體,功能完全正常。`部署說明.md` 裡有免安裝的私有註冊寫法。

驗收:兩台各跑一次 `verify_deploy.py` 對 sha256,再跑同一張 invoice 對總金額。
第二步才是真的驗完。

---

## 一個更正

我先前說 `dim_factor = 300` 對照 `default_dim_factor = 139`「洩漏一項重大議價成果」,
**這是錯的**。300 是程式第 4835 行寫死的預設值。同批的還有
`fuel_percent 18.75`(4841)、`residential_fee 5`(4893)、`ahs_weight_fee 34`(4900)、
`oversize_fee 205`(4903),全是內建預設,不是議價結果。

所以你後來上傳的那份 9.6 KB 空白 config 完全沒有客戶資料,可以直接進 git,
連這個包裡的 sample 都不需要用。真正敏感的是 413 KB 那份。
