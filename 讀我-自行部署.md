# UPS Reprice — 自行部署

整個工具就是 `site/index.html` 一份靜態網頁。**沒有 build、沒有 Node、沒有資料庫。**
把 `site/` 裡的檔案原封不動放到任何一台能提供網頁的機器上,就跑得起來。

也因為沒有 build,所謂「部署」就是複製檔案 —— 不會有任何平台的每日部署額度。

---

## 一、硬性條件:必須是 https

不是建議,是**做不到就不能用**。這個網頁用到三個瀏覽器 API,它們一律只在
安全環境(https 或 localhost)提供:

| API | 用在哪裡 | 走 http 會怎樣 |
|---|---|---|
| `crypto.subtle` | 登入時比對密碼雜湊 | **完全無法登入**,畫面會直接說「登入需要 https」 |
| `showSaveFilePicker` | 匯出 Excel 時選存檔位置 | 退回一般下載,還能用 |
| `navigator.clipboard` | 複製按鈕 | 複製失效 |

用 `http://192.168.x.x/` 開會卡在登入畫面。**內部網站也要憑證** —— 可以用
內部 CA 簽,或用 Caddy 自動申請(見下)。

例外:`http://localhost/` 本身算安全環境,所以在自己電腦上測試不必弄憑證。

---

## 二、檔案放哪裡

`site/` 底下五個檔案**放在同一層**,`index.html` 在網站根目錄:

```
/var/www/ups/            ← 或 IIS 的網站根目錄
├── index.html                    工具本體
├── auth-config.js                登入模式設定
├── supabase-js-2.112.3.js        登入用的元件
├── xlsx-0.18.5.full.min.js       匯出 Excel 用,按下匯出才會載
└── UPS-API-Setup-Guide.pdf       設定頁那顆「API 設定指南」下載的就是它
```

五個都要。少了 `auth-config.js` 或 `supabase-js` 網頁開不起來;少了 `xlsx`
匯出會失敗並提示;少了 PDF 則是按下下載時會告訴你檔案不在。

---

## 三、伺服器設定

`server-configs/` 裡有四種常見伺服器的範例,挑你用的那一種:

| 檔案 | 用在 |
|---|---|
| `Caddyfile.example` | **Caddy** —— 最省事,憑證自動申請與續期 |
| `nginx.conf.example` | nginx |
| `web.config` | IIS(Windows Server),放進網站根目錄即可 |
| `.htaccess` | Apache(需要 mod_headers、mod_rewrite) |

每一份都只做兩件事:**http 轉 https**,以及**讓 `index.html` 不要被瀏覽器快取住**。

第二件事比看起來重要。整個工具就是那一份 `index.html` —— 被快取住的話,你換了
新版,開網頁的人還是拿到舊的,而且**畫面上看不出來**:網頁照常開啟,只是
一整批功能像是沒做過。設定成 `no-cache` 就沒有這個問題(那不是「不准快取」,
是「每次先問伺服器有沒有變」:沒變回 304,幾乎不花流量)。

---

## 四、部署完怎麼確認拿到的是新版

滑鼠移到標題列的 **DEVELOPED BY SANDY LIU** 上,tooltip 會顯示這份
`index.html` 的最後修改時間。(也可以開 F12 主控台,載入時會印一行
`UPS Reprice — build ...`。)

**日期沒有跟著這次部署往前走,就是沒有拿到新檔**,不必再一個一個功能去找
哪裡不見了。分兩種情況:

| 現象 | 原因 | 處理 |
|---|---|---|
| 網址後面加 `?v=1` 就變新的 | 瀏覽器或 CDN 的快取 | 伺服器設定沒套用到,對照第三節;臨時可按 Ctrl+Shift+R |
| 加了 `?v=1` 還是舊的 | 主機上的檔案本身是舊的 | 檔案沒複製過去,或複製到別的資料夾 |

---

## 五、帳號與密碼

目前是**本機帳號模式**:帳號直接寫在 `index.html` 裡的 `LOCAL_ACCOUNTS`,
存的是 `sha256(帳號 + ":" + 密碼)` 的雜湊,不是明碼。

要改密碼或加人,就換一組新的雜湊值。產生方式(在瀏覽器主控台執行):

```js
crypto.subtle.digest("SHA-256", new TextEncoder().encode("帳號:新密碼"))
  .then(b => console.log([...new Uint8Array(b)]
    .map(x => x.toString(16).padStart(2,"0")).join("")));
```

把印出來的字串填回 `LOCAL_ACCOUNTS` 對應那一筆的 `hash`。

**要說清楚:這是純前端的鎖,擋君子不擋小人。** 看得到這份 HTML 的人就看得到
帳號清單與雜湊。真正要緊的東西(合約費率、帳單明細)存在各自瀏覽器裡,
不在這份檔案裡,所以不經過這道門 —— 但這也表示**這個網址本身不該公開**,
內部網路或加一層基本驗證比較妥當。

---

## 六、之後若要改用 Supabase 登入

在 `auth-config.js` 加一行 `authMode: "supabase"` 即可切換,程式不用動。

但**切之前要先做資料搬遷**:每個帳號的資料是綁登入 id 存的,本機模式是
`local::<帳號>`,Supabase 模式是一組 UUID —— 兩者不同,切過去之後費率、
帳單索引、明細會全部「看起來不見了」(東西還在舊 key 底下,只是畫面上是空的)。
這段搬遷還沒寫,先別切。

另外 Supabase 那邊要把新網址加進
`Authentication → URL Configuration` 的允許清單,否則登入會被擋。

---

## 七、UPS 地址驗證 API(選用)

那是另一件事,和這裡的部署無關 —— 它需要一個 Supabase Edge Function 當代理。
完整步驟在 `site/UPS-API-Setup-Guide.pdf`(也就是設定頁那顆按鈕下載的那一份)。
沒有設定的話,住宅/商業一律用系統判定,工具照常運作。
