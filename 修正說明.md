# index.html 對齊 UPS_Reprice_Tool.py — 25 項修正

基準：`UPS_Reprice_Tool.py`（你上傳的版本）。每一項都在兩份原始碼上核對過行號，
並用 15 個合成情境在 Node 與 Chromium 上各跑過一次，兩邊數字一致。

檔案：`index.html`（直接覆蓋倉庫根目錄那一份即可，其餘檔案不用動）

---

## A 級 — 原本完全沒有實作的收費

| # | 項目 | 改了什麼 |
|---|---|---|
| A1 | 簽名費 / 成人簽名費 | `ACC_CODE_MAP` 補上 `CSG` `DCS` `SIG` → Signature，`ADS` `ASG` → Adult Signature |
| A2 | 超大件 OVR / OML | 補上代碼對照，並照 py `:19980` 在查不到合約費率時退回 UPS 實收金額；Demand 的 `OVR > LPS > AHS` 優先序因此恢復 |
| A3 | 地址更正 ADC | `isFreightLine` 排除 ADJ+ADC；`scanCharges` 在 MAIN_LINE 之前先攔下 ADJ 明細碼；服務別與 Zone 的搜尋都跳過附加費行 |
| A4 | 第二哩運費 | DIN 攔截段或 ISW 退回寄件人時，用回程自己的 Zone 再查一次底價寫進 `2nd Mile Road Base Rate`；純 DIN 且沒有第一趟時搬回 Base Rate |
| A5 | SHP + RTN 兩段運費 | RTN 行照 CLB 的做法拆成獨立一段各自計價，合併報表時運費進 `Undeliverable Return`，附加費與燃油相加 |

## B 級 — 核心計價

| # | 項目 | 改了什麼 |
|---|---|---|
| B1 | 費率級距 | 改成整數精準比對（py `:17052`）。表上跳號時查無此列＝0；計費重 0 不再退化成表上最低那一列 |
| B2 | 查不到費率 | 五個失敗出口全部回 `rate:0, missing:true`，附加費與燃油照收，Total 不再整列歸零 |
| B3 | 住／商費率表 | 先試指定旗標，再試相反旗標；候選名加上不帶後綴的中性名；舊格式 `residential:null` 的表兩種旗標都收 |
| B4 / B6 | 實重取法 | 改成 `max(帳單計費重, 磅秤重量)`（py `:18158`），只有在「UPS 的數字被判定為附加費最低重量的殘留、而且手上有尺寸可重判」時才只用磅秤重量 |
| B5 | BWT 2 / 8 | 這兩型不再套材積重（py `:18443`）；材積重也改成先四捨五入到小數兩位再進位 |
| B7 | 服務名稱辨識 | 從 5 條擴到 py 的完整 40 條、涵蓋 15 種服務；全部落空時退回設定裡的預設服務而不是原始描述 |

## C 級 — 調整層

| # | 項目 | 改了什麼 |
|---|---|---|
| C1 | 沖銷行正負號 | AHS 三桶與大包裹：UPS 那一行淨額為負時寫 `-abs(費率)`（py `_signed` `:19939`）。住宅／DAS／偏遠不翻符號 |
| C2 | 1×1×1 | `parseDims` 直接回 null，全流程視為沒有尺寸 |
| C3 | 商業重分類純 ADJ | 住宅費寫負值退款；有 DAS 時寫 `商業DAS − 住宅DAS` 的差額 |
| C4 | DIN 的 Zone | 主 Zone 排除 DIN 行，只有主 Zone 空著時才拿它當最後手段 |
| C5 | 住／商判定順序 | 商業重分類 → 服務名稱明講 → 跨期 ADJ 查歷史 → 帳單代碼 → 服務名稱 |
| C6 | 純 ADJ 重量對 | 新增 `rate(billed) − rate(entered)` 的差額分支（py `:18712`）；同時移除 py 沒有的「用歷史 CSV 重建 A 件抵差」路徑 |

## D 級 — 查表、燃油、報表

| # | 項目 | 改了什麼 |
|---|---|---|
| D1 | 附加費 Zone | 非標準 Zone 併進「8」（py `:17128`），passthrough 取自標準組加各渠道宣告的 Zone |
| D2 | 附加費鍵名 | 先試「渠道 + Commercial/Residential」再試不帶後綴的 |
| D3 | 燃油日期 | 有 ADJ 層就用 ADJ 那一層自己的 Transaction Date（py `:20590` 的 ADJ → SHP → RTN 順序）|
| D4 | Demand 觸發 | 改成看「這一層有沒有帶運費」，不是「有沒有查到費率」 |
| D5 | Demand 欄位 | 報表補上 `Demand - AHS / Large Package / Over Maximum / Service` 四欄 |
| D6 | 四捨五入 | 匯出時改用 `Number(v.toFixed(2))`，不再先放大再進位 |
| D7 | 相容性 | 補舊版住宅費鍵名的退路；`schedDate` 補 `YYYYMMDD`；門檻為 0 的語意改成和 py 一致 |

---

## 三個要你確認的判斷

這三處我照 py 做了，但行為和以前差很多，上線前值得先用一期真實帳單看一眼。

**1. 門檻是 0 時會對每一件都觸發。** py 讀不到門檻就當 0.0，而且沒有任何 `> 0` 的保護，
於是 `尺寸 > 0` 對每一件都成立＝全部都算超大件／AHS。網頁版以前多加了一道保護當成「停用」。
現在改成照 py，但會在 issue 欄列出「這些門檻是 0」。你的設定檔每個渠道都填齊了，
所以實際上不會有差；但如果之後有人清空某一格，兩邊會一起爆而不是只有一邊。

**2. 商業重分類的獨立更正單現在會收一筆完整底價。** py 的 `cal_base` 在這種單子上就是
`freight_C` 全額（`Residential Adjustment` / `Commercial Adjustment` 兩欄是算了但不進總額的）。
我的測試案例上，UPS 淨額是 −15.10、我們算出 +6.11。這是 py 的行為，不是 bug，但數字落差大。

**3. 移除了「用歷史 CSV 重建原始包裹來抵差」。** 這是網頁版獨有的，py 的
`history_service_for` 只回渠道、`history_zone_for` 只回 Zone，從來不重建一個 A 件。
留著會算出 py 不會算的差額。如果你其實想保留這個行為，告訴我，我把它加回去並在 py 那邊補上對應邏輯。

---

## 驗證

15 個合成情境（簽名費、地址更正、超大件、DIN 攔截、SHP+RTN、BWT 2、
帳單計費重大於實重、超出費率表、AHS 沖銷、1×1×1、無尺寸重量對、DIN 排在前面、
商業重分類、計費重 0、成人簽名）在改前改後各跑一次，49 格有差異，
每一格都對得上上面某一項修正；沒有一格是意料之外的變動。

Chromium 實際載入兩份 index.html 都沒有 JS 執行錯誤。

正式上線前建議：拿 346 那一期的 CSV，用桌面版和這份網頁版各跑一次，
逐格比對匯出的 xlsx。
